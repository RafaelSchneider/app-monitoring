import 'package:flutter/material.dart';

class MyThirdHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("Third Page");
    return Container(
      child: Text("Testando pagina 3"),
    );
  }
}